fn main() {
  let a: Int = 42;
  println(Int(Uint(a)));    // 42
  println(Int(Int8(a)));    // 42
  println(Int(Int64(a)));   // 42

  let b: Uint = 10u;
  println(Int(b));          // 10
  println(Int(Int32(b)));   // 10

  let c: Int8 = 7i8;
  println(Int(c));          // 7
  println(Int(Uint(c)));    // 7
  println(Int(Int64(c)));   // 7

  let d: Int = 5;
  let e: Float64 = Float64(d);
  println(Int(e));          // 5
}
