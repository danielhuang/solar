fn main() {
  // closure parameter type inferred as Uint
  let arr = [5u; \i i];
  println(arr[0u]);  // 0
  println(arr[1u]);  // 1
  println(arr[4u]);  // 4

  // inferred parameter, expression body
  let squares = [4u; \i i * i];
  println(squares[0u]);  // 0
  println(squares[1u]);  // 1
  println(squares[2u]);  // 4
  println(squares[3u]);  // 9

  // inferred parameter with capture
  let offset = 10u;
  let arr2 = [3u; \i i + offset];
  println(arr2[0u]);  // 10
  println(arr2[1u]);  // 11
  println(arr2[2u]);  // 12
}
