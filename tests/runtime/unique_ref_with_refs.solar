fn main() {
    let a = 5^;
    
    // references allowed to alias unique references
    let a_ref = a@&;
    
    a_ref@ = 6;
    println(a@); // 6
    
    // unique references cannot alias each other
    let a2 = a@^;
    a2@ = 7;
    println(a@); // 6
    println(a2@); // 7
}
