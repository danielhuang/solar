// The `catch` binding must be `Exception`; an explicit wrong annotation is a
// type error (the handler desugars to a `fn(Int)`, but `try` wants `fn(Exception)`).
fn main() {
	try {} catch (e: Int) {}
}
