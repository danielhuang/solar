pub struct Holder { pub v: Int, }

// Same bare name as the free function in funcs.solar. Methods are resolved
// globally as overload sets, so this must not displace the function.
pub method shared_name(self: &Holder) -> Int { self@.v }
