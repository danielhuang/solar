fn main() {
  let a = 0;
  let a_fn = \ a;

  let b = 0&;
  let b_fn = \ b@;

  let my_fn = a_fn;

  thread::spawn(\ {
    while true {
      my_fn = a_fn;
      my_fn = b_fn;
    }
  });

  while true {
    my_fn();
  }
}
